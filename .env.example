# Base API host (no trailing slash)
VITE_API_URL=http://localhost:8001

# Providers prefix (match FastAPI PROVIDERS_API_PREFIX)
VITE_API_PREFIX=/api-providers

# Chat API base (direct port in dev; proxy adds /api-chat in prod)
VITE_CHAT_API_URL=http://localhost:8002
